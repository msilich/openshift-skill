# Optional config-dir drop-in for Day-2 tasks that must not access raw Secrets.
# Copy it to a protected config-dir, add --config-dir <that-directory> to the
# MCP command, and restart the MCP process after the user selects this policy.
# Source baseline: openshift/openshift-mcp-server@a0a1c370fcce18f76cecdbb2e07d8f4dbafe92dd

[[denied_resources]]
group = ""
version = "v1"
kind = "Secret"
